#!/usr/bin/env bash

# this is file used to autostart program with dwm patch with 'autostart patch'

# currently there's only those tools/utilities which are not executed by ~/.xprofile reason(IDK why)

mpd_changes.sh &
caffeine_like &
picom &
